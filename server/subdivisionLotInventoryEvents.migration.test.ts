import{describe,it,expect}from"vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(import.meta.dirname,"../supabase/migrations/20260828009500_subdivision_lot_inventory_events_a22.sql"),"utf8");describe("A22 inventory history",()=>{it("keeps transitions contextual and server-only",()=>{expect(sql).toContain("SUBDIVISION_INVENTORY_TRANSITION_DENIED");expect(sql).toContain("payload_redacted");expect(sql).toContain("to service_role")})});
