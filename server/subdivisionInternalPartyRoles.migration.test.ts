import{describe,it,expect}from"vitest";import{readFileSync}from"node:fs";import{resolve}from"node:path";const sql=readFileSync(resolve(import.meta.dirname,"../supabase/migrations/20260828009000_subdivision_internal_party_roles_a21.sql"),"utf8");describe("A21 internal subdivision roles",()=>{it("keeps roles contextual and server-only",()=>{expect(sql).toContain("SUBDIVISION_INTERNAL_ROLE_CONTEXT_DENIED");expect(sql).toContain("payload_redacted");expect(sql).toContain("to service_role");expect(sql).toContain("shareholder");expect(sql).toContain("land_contributor")})});
