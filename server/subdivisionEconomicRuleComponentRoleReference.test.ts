import { describe, expect, it, vi } from "vitest";
import { addSubdivisionEconomicRuleComponentRoleReference, listSubdivisionEconomicRuleComponentRoleReferences } from "./subdivisionEconomicRuleComponentRoleReference";

const input = { organizationId: "00000000-0000-4000-8000-000000000002", module: "loteadora" as const, purposeCode: "CADASTRO_INICIAL", economicRuleComponentId: "00000000-0000-4000-8000-000000000003", internalPartyRoleLinkId: "00000000-0000-4000-8000-000000000004", correlationId: "00000000-0000-4000-8000-000000000005" };
describe("subdivision economic rule component role reference", () => { it("requires a subject before any RPC", async () => { const client = { rpc: vi.fn() } as never; await expect(addSubdivisionEconomicRuleComponentRoleReference(undefined, input, client)).rejects.toThrow("SUBDIVISION_IDENTITY_REQUIRED"); expect(client.rpc).not.toHaveBeenCalled(); }); it("keeps the returned reference minimized and non-financial", async () => { const client = { rpc: vi.fn().mockResolvedValueOnce({ data: "00000000-0000-4000-8000-000000000006", error: null }).mockResolvedValueOnce({ data: [{ economic_rule_component_role_reference_id: "00000000-0000-4000-8000-000000000006", economic_rule_component_id: input.economicRuleComponentId, internal_party_role_link_id: input.internalPartyRoleLinkId, created_at: "2026-08-28T00:00:00.000Z", amount: 100 }], error: null }) } as never; await expect(addSubdivisionEconomicRuleComponentRoleReference("00000000-0000-4000-8000-000000000001", input, client)).resolves.toEqual({ economicRuleComponentRoleReferenceId: "00000000-0000-4000-8000-000000000006" }); const result = await listSubdivisionEconomicRuleComponentRoleReferences("00000000-0000-4000-8000-000000000001", input, client); expect(result[0]).toMatchObject({ economicRuleComponentId: input.economicRuleComponentId, internalPartyRoleLinkId: input.internalPartyRoleLinkId }); expect(result[0]).not.toHaveProperty("amount"); }); });
