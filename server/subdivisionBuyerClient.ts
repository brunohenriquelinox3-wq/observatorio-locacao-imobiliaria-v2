import type { SupabaseClient } from "@supabase/supabase-js";
import { draftSubdivisionBuyerClientInputSchema, subdivisionContextSchema, type DraftSubdivisionBuyerClientInput } from "../shared/subdivisionContracts";
import { getSupabaseAdminClient } from "./supabase";
type RpcClient = Pick<SupabaseClient, "rpc">;
export type DraftSubdivisionBuyerClientSummary = { buyerClientId: string; partyRoleAssignmentId: string; createdAt: string };
function requireSubject(subjectId: string | undefined): string { if (!subjectId) throw new Error("SUBDIVISION_IDENTITY_REQUIRED"); return subjectId; }
export async function listDraftSubdivisionBuyerClients(subjectId: string | undefined, rawContext: unknown, client: RpcClient = getSupabaseAdminClient()): Promise<DraftSubdivisionBuyerClientSummary[]> { const actorUserId=requireSubject(subjectId);const context=subdivisionContextSchema.parse(rawContext);const {data,error}=await client.rpc("subdivision_list_draft_buyer_clients",{p_actor_user_id:actorUserId,p_organization_id:context.organizationId,p_module:context.module,p_purpose_code:context.purposeCode});if(error||!Array.isArray(data))throw new Error("SUBDIVISION_BUYER_CLIENT_READ_DENIED");return data.map((row)=>({buyerClientId:String(row.buyer_client_id),partyRoleAssignmentId:String(row.party_role_assignment_id),createdAt:String(row.created_at)})); }
export async function createDraftSubdivisionBuyerClient(subjectId: string | undefined, rawInput: DraftSubdivisionBuyerClientInput, client: RpcClient = getSupabaseAdminClient()): Promise<{buyerClientId:string}>{const actorUserId=requireSubject(subjectId);const input=draftSubdivisionBuyerClientInputSchema.parse(rawInput);const {data,error}=await client.rpc("subdivision_create_draft_buyer_client",{p_actor_user_id:actorUserId,p_organization_id:input.organizationId,p_module:input.module,p_purpose_code:input.purposeCode,p_party_role_assignment_id:input.partyRoleAssignmentId,p_correlation_id:input.correlationId});if(error||typeof data!=="string")throw new Error("SUBDIVISION_BUYER_CLIENT_COMMAND_DENIED");return{buyerClientId:data};}
