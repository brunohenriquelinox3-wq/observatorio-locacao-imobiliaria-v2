import type{SupabaseClient}from"@supabase/supabase-js";import{subdivisionContextSchema}from"../shared/subdivisionContracts";import{getSupabaseAdminClient}from"./supabase";type RpcClient=Pick<SupabaseClient,"rpc">;const subject=(v:string|undefined)=>{if(!v)throw new Error("SUBDIVISION_IDENTITY_REQUIRED");return v};
export async function listDraftSubdivisionInternalPartyRoles(id:string|undefined,raw:unknown,client:RpcClient=getSupabaseAdminClient()){const actor=subject(id),c=subdivisionContextSchema.parse(raw);const{data,error}=await client.rpc("subdivision_list_draft_internal_party_roles",{p_actor_user_id:actor,p_organization_id:c.organizationId,p_module:c.module,p_purpose_code:c.purposeCode});if(error||!Array.isArray(data))throw new Error("SUBDIVISION_INTERNAL_ROLE_READ_DENIED");return data.map(r=>({linkId:String(r.link_id),developmentId:String(r.development_id),partyRoleId:String(r.party_role_assignment_id),role:String(r.role),startsAt:String(r.starts_at),endsAt:r.ends_at?String(r.ends_at):null}))}
export async function linkDraftSubdivisionInternalPartyRole(id:string|undefined,input:{organizationId:string;module:"loteadora";purposeCode:string;developmentId:string;partyRoleId:string;correlationId:string},client:RpcClient=getSupabaseAdminClient()){const actor=subject(id),c=subdivisionContextSchema.parse(input);const{data,error}=await client.rpc("subdivision_link_draft_internal_party_role",{p_actor_user_id:actor,p_organization_id:c.organizationId,p_module:c.module,p_purpose_code:c.purposeCode,p_development_id:input.developmentId,p_party_role_assignment_id:input.partyRoleId,p_correlation_id:input.correlationId});if(error||typeof data!=="string")throw new Error("SUBDIVISION_INTERNAL_ROLE_COMMAND_DENIED");return{linkId:data}}
