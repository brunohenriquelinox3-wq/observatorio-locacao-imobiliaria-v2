import type { SupabaseClient } from "@supabase/supabase-js";
import { getSupabaseAdminClient } from "./supabase";
import { storagePut } from "./storage";
type RpcClient = Pick<SupabaseClient, "rpc">;
type PrivateStorageWriter = (key: string, bytes: Buffer, contentType: string) => Promise<{ key: string }>;
const allowedTypes = new Set(["application/pdf", "image/jpeg", "image/png"]); const maxBytes = 2 * 1024 * 1024;
function signatureMatches(contentType: string, bytes: Buffer) { return (contentType === "application/pdf" && bytes.subarray(0, 5).equals(Buffer.from("%PDF-"))) || (contentType === "image/jpeg" && bytes.subarray(0, 3).equals(Buffer.from([0xff, 0xd8, 0xff]))) || (contentType === "image/png" && bytes.subarray(0, 8).equals(Buffer.from([0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a]))); }
export async function storePrivateSaleCaseDossierAttachment(input: { subjectId?: string; organizationId: string; purposeCode: string; attachmentIntentId: string; correlationId: string; contentType: string; bytes: Buffer }, client: RpcClient = getSupabaseAdminClient(), writePrivateObject: PrivateStorageWriter = storagePut) { if (!input.subjectId) throw new Error("SUBDIVISION_IDENTITY_REQUIRED"); if (!allowedTypes.has(input.contentType) || input.bytes.byteLength < 1 || input.bytes.byteLength > maxBytes || !signatureMatches(input.contentType,input.bytes)) throw new Error("SUBDIVISION_SALE_CASE_ATTACHMENT_REJECTED"); const authorized = await client.rpc("subdivision_authorize_sale_case_document_upload", { p_actor_user_id: input.subjectId,p_organization_id: input.organizationId,p_module: "loteadora",p_purpose_code: input.purposeCode,p_attachment_intent_id: input.attachmentIntentId }); if (authorized.error || typeof authorized.data !== "string") throw new Error("SUBDIVISION_SALE_CASE_ATTACHMENT_DENIED"); const stored = await writePrivateObject(`private/${input.organizationId}/sale-case-attachments/${input.attachmentIntentId}`,input.bytes,input.contentType); const { data,error } = await client.rpc("subdivision_record_sale_case_document_upload", { p_actor_user_id: input.subjectId,p_organization_id: input.organizationId,p_module: "loteadora",p_purpose_code: input.purposeCode,p_attachment_intent_id: input.attachmentIntentId,p_storage_key: stored.key,p_content_type: input.contentType,p_byte_size: input.bytes.byteLength,p_correlation_id: input.correlationId }); if (error || typeof data !== "string") throw new Error("SUBDIVISION_SALE_CASE_ATTACHMENT_DENIED"); return { attachmentIntentId: data }; }
