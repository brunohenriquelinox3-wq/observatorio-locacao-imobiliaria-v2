import DashboardLayout,{type DashboardAccessGate,type DashboardNavigationItem}from"@/components/DashboardLayout";import{useAuth}from"@/_core/hooks/useAuth";import{isDomainContextReady}from"@/lib/domainFoundationUi";import{trpc}from"@/lib/trpc";import{CircleAlert,Compass,LandPlot,Map,ShieldCheck,Workflow}from"lucide-react";import{useMemo,useState}from"react";import{toast}from"sonner";import"../subdivision-foundation.css";
const nav:DashboardNavigationItem[]=[{icon:Compass,label:"Central de plataforma",path:"/administracao"},{icon:LandPlot,label:"Loteadora",path:"/loteadora"},{icon:Map,label:"Estoque/Mapa de Lotes",path:"/estoque-lotes"},{icon:Workflow,label:"Vendas Urbanas",path:"/vendas-urbanas"}];const gate:DashboardAccessGate={eyebrow:"ESTOQUE/MAPA RESTRITO · CONTEXTO ANTES DE LEITURA",title:"Acesse o cadastro-base de Lotes somente no seu contexto autorizado.",description:"Esta área registra posições de rascunho e situação interna. O servidor verifica identidade, membership, grant, vigência, finalidade e policy.",routeTitle:"Rota de acesso",routeDetail:"Autenticação → contexto → escopo vigente → policy",actionLabel:"Acessar Estoque/Mapa de Lotes",footerLabel:"LEITURA PROTEGIDA",footerValue:"CONTEXTO · NÃO INFERÊNCIA",footerNote:"Nenhum lote ou registro de outro contexto é exposto antes da autorização.",railTop:"OPERAÇÃO",railBottom:"ESTOQUE"};
export default function LotInventory(){const{isAuthenticated}=useAuth();const[organizationId,setOrganizationId]=useState("");const[purposeCode,setPurposeCode]=useState("CADASTRO_INICIAL");const[blockId,setBlockId]=useState("");const[lotNumber,setLotNumber]=useState(1);const[lotId,setLotId]=useState("");const[phase,setPhase]=useState<"reference_confirmed"|"structure_review"|"review_required">("reference_confirmed");const context=useMemo(()=>({organizationId:organizationId.trim(),module:"loteadora"as const,purposeCode:purposeCode.trim().toUpperCase()}),[organizationId,purposeCode]);const contextReady=isAuthenticated&&isDomainContextReady(context);const ready=contextReady&&!!blockId;const input=useMemo(()=>({...context,blockId}),[context,blockId]);const lots=trpc.subdivisionFoundation.listDraftLots.useQuery(input,{enabled:ready,retry:false});const events=trpc.subdivisionFoundation.listDraftLotInventoryEvents.useQuery(context,{enabled:contextReady,retry:false});const utils=trpc.useUtils();const create=trpc.subdivisionFoundation.createDraftLot.useMutation({onSuccess(){setLotNumber(1);void utils.subdivisionFoundation.listDraftLots.invalidate(input);toast.success("Lote em rascunho registrado")}});const inventory=trpc.subdivisionFoundation.upsertDraftLotInventoryState.useMutation({onSuccess(){void events.refetch();toast.success("Situação interna registrada")}});return <DashboardLayout navigationItems={nav} navigationTitle="Núcleo CRM" accessGate={gate}><main className="subdivision-foundation-page"><header className="subdivision-foundation-hero"><div><p className="subdivision-foundation-eyebrow">ESTOQUE/MAPA DE LOTES · CADASTRO-BASE</p><h1>Quadra N · Lote M, sem prometer disponibilidade.</h1><p>O setor separado registra posição numérica e situação interna, sem mapa, preço, reserva, cliente, contrato ou financeiro.</p></div><div className="subdivision-foundation-hero__rule"><ShieldCheck size={18}/><span>Rascunho contextual<br/><b>máximo de 100 lotes por Quadra</b></span></div></header><section className="subdivision-foundation-context"><div className="subdivision-foundation-context__fields"><label>Organização<input value={organizationId} onChange={e=>setOrganizationId(e.target.value)}/></label><label>Módulo<input value="Loteadora" readOnly/></label><label>Finalidade<input value={purposeCode} onChange={e=>setPurposeCode(e.target.value.toUpperCase())}/></label></div></section><section className="subdivision-foundation-workspace"><form className="subdivision-foundation-card" onSubmit={e=>{e.preventDefault();create.mutate({...context,correlationId:crypto.randomUUID(),blockId,lotNumber})}}><h3>Novo Lote em rascunho</h3><input value={blockId} onChange={e=>setBlockId(e.target.value)} placeholder="UUID da Quadra" required/><input type="number" min={1} max={100} value={lotNumber} onChange={e=>setLotNumber(Number(e.target.value))}/><button disabled={!ready}>Registrar Lote</button></form>{!ready&&<div className="subdivision-foundation-empty"><CircleAlert size={18}/><p>Contexto, sessão e Quadra válida são necessários.</p></div>}{lots.data?.map(l=><article key={l.lotId}><h3>Lote {l.lotNumber}</h3><code>{l.lotId}</code></article>)}<form className="subdivision-foundation-card" onSubmit={e=>{e.preventDefault();inventory.mutate({...context,correlationId:crypto.randomUUID(),lotId,inventoryPhase:phase})}}><h3>Situação interna de inventário</h3><input value={lotId} onChange={e=>setLotId(e.target.value)} placeholder="UUID do Lote" required/><select value={phase} onChange={e=>setPhase(e.target.value as typeof phase)}><option value="reference_confirmed">Referência confirmada</option><option value="structure_review">Revisão de estrutura</option><option value="review_required">Revisão necessária</option></select><button disabled={!contextReady||inventory.isPending}>Registrar situação interna</button></form></section><section className="subdivision-foundation-list"><h2>Histórico interno de inventário</h2><p>Registra somente transições internas autorizadas; não demonstra disponibilidade, reserva, venda ou dados de cliente.</p>{contextReady&&events.data?.map(e=><article key={e.eventId}><b>{e.fromPhase} → {e.toPhase}</b><p>Evento interno em {new Date(e.createdAt).toLocaleString("pt-BR")}</p></article>)}</section></main></DashboardLayout>}
